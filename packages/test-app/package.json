{
  "name": "test-app",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "lint": "eslint .",
    "preview": "vite preview",
    "initialize": "npx symlink-dir ../common/src ./src/common"
  },
  "dependencies": {
    "ts-react-app-base": "*"
  }
}
